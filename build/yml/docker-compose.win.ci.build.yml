version: '3.4'

services:
  ci-build:
    image: microsoft/dotnet:2.1-sdk
    volumes:
      - "..\\..\\:c:\\ProgramData"
    working_dir: "c:\\ProgramData"
    command: dotnet publish "src\MunCode.munERP.sln" -c Debug -v normal